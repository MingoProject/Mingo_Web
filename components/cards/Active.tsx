import React from "react";
import Button from "../ui/button";

const Active = () => {
  return (
    <Button
      title="Active"
      // className="w-12 text-xs md:w-20 cursor-default h-fit text-green-500 rounded-lg bg-custom-green"
    />
  );
};

export default Active;
