import React from "react";
import { Button } from "../ui/button";

const InProgress = () => {
  return (
    <Button className="h-fit w-12 cursor-default  rounded-lg  bg-light-red  text-xs font-semibold text-red-600 md:w-20">
      In progress
    </Button>
  );
};

export default InProgress;
